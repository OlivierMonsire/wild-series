{% extends 'base.html.twig' %}

{% block title %}{{ 'show.edit'|trans }} {{ 'category.episode'|trans }}{% endblock %}

{% block body %}
    <h1>{{ 'show.edit'|trans }} {{ 'category.episode'|trans }}</h1>

    {{ include('episode/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-secondary" href="{{ path('episode_index') }}">{{ 'show.back'|trans }}</a>

    {{ include('episode/_delete_form.html.twig') }}
{% endblock %}
