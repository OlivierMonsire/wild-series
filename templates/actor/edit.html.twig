{% extends 'base.html.twig' %}

{% block title %}{{ 'show.edit'|trans }} {{ 'category.actor'|trans }}{% endblock %}

{% block body %}
    <h1>{{ 'show.edit'|trans }} {{ 'category.actor'|trans }}</h1>

    {{ include('actor/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-secondary" href="{{ path('actor_index') }}">{{ 'show.back'|trans }}</a>

    {{ include('actor/_delete_form.html.twig') }}
{% endblock %}
