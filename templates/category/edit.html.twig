{% extends 'base.html.twig' %}

{% block title %}{{ 'show.edit'|trans }} {{ 'category.category'|trans }}{% endblock %}

{% block body %}
    <h1>{{ 'show.edit'|trans }} {{ 'category.category'|trans }}</h1>

    {{ include('category/_form.html.twig', {'button_label': 'update'}) }}

    <a class="btn btn-secondary" href="{{ path('category_index') }}">{{ 'show.back'|trans }}</a>

    {{ include('category/_delete_form.html.twig') }}
{% endblock %}
