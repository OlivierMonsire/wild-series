{% extends 'base.html.twig' %}

{% block title %}{{ 'show.new'|trans }} {{ 'category.season'|trans }}{% endblock %}

{% block body %}
    <h1>{{ 'show.new'|trans }} {{ 'category.season'|trans }}</h1>

    {{ include('season/_form.html.twig') }}

    <a class="btn btn-secondary" href="{{ path('season_index') }}">{{ 'show.back'|trans }}</a>
{% endblock %}
