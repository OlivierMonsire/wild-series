{% extends 'base.html.twig' %}

{% block title %}{{ 'show.edit'|trans }} {{ 'category.season'|trans }}{% endblock %}

{% block body %}
    <h1>{{ 'show.edit'|trans }} {{ 'category.season'|trans }}</h1>

    {{ include('season/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-secondary" href="{{ path('season_index') }}">{{ 'show.back'|trans }}</a>

    {{ include('season/_delete_form.html.twig') }}
{% endblock %}
